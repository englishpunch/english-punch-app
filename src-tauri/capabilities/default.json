{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "opener:allow-open-path",
    "oauth:allow-start",
    "oauth:allow-cancel",
    {
      "identifier": "opener:allow-open-path",
      "allow": [{ "path": "https://**" }, { "path": "http://**" }]
    }
  ]
}
