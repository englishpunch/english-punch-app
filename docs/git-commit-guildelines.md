## Commit Rules

Write commits in Korean following the repo's Korean Conventional profile. Use `<type>(<scope>): <description>`; add a body only when essential.

## Types

Allowed types: `feat`, `fix`, `refactor`, `chore`, `docs`, `style`, `perf`. Use `refactor` solely for behavior-neutral structure changes.

